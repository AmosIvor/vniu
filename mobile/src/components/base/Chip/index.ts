import FillChip from './FillChip';
import GhostChip from './GhostChip';
import OutLineChip from './OutLineChip';

export const Chip = {
  Fill: FillChip,
  OutLine: OutLineChip,
  Ghost: GhostChip,
};
